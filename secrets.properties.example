# 이 파일을 secrets.properties 로 복사한 뒤 값을 채운다.
# secrets.properties 는 git에 올라가지 않는다.
DB_USERNAME=<DB 사용자명>
DB_PASSWORD=<DB 비밀번호>
# HS256 서명용 랜덤 키, 32바이트 이상. 생성 예: openssl rand -base64 48
JWT_SECRET=<시크릿 키>
